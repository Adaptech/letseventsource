workspace:
  base: /go
  path: src/github.com/Adaptech/letseventsource

pipeline:
  build:
    image: golang:latest
    commands:
      - apt-get update && apt-get install hugo
      - cd docs && make all


